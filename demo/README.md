# Legacy Static Demo

`demo/` 是早期静态 `index.html + app.js + styles.css` 版本，保留作迁移参考。

当前主演示入口已经切换到独立 Vue/Vite 小应用：

```text
C:\code\career\agent\capability-assessment\app
```

启动方式见：

```text
agent/capability-assessment/README.md
```
